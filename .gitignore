venv/
venv_win/
__pycache__/
*.pyc

# Ignore raw/consolidated data files but keep trained models
data/raw/
data/treated/
data/results/

# Keep models_trained/ (tracked by git for deployment)
# data/models_trained/ is NOT ignored

*.egg-info/
dist/
build/
